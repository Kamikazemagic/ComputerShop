<Page x:Class="ComputerShop.RegisterForm"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ComputerShop"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RegisterForm">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Felhasználónév -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Felhasználónév:" Margin="0,0,5,10" VerticalAlignment="Center"/>
        <TextBox x:Name="userNameTextBox" Grid.Row="0" Grid.Column="1" Width="200" Margin="0,0,0,10"/>

        <!-- Jelszó -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Jelszó:" Margin="0,0,5,10" VerticalAlignment="Center"/>
        <PasswordBox x:Name="userPasswordTextBox" Grid.Row="1" Grid.Column="1" Width="200" Margin="0,0,0,10"/>

        <!-- Teljes név -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Teljes név:" Margin="0,0,5,10" VerticalAlignment="Center"/>
        <TextBox x:Name="fullNameTextBox" Grid.Row="2" Grid.Column="1" Width="200" Margin="0,0,0,10"/>

        <!-- Email -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Email:" Margin="0,0,5,10" VerticalAlignment="Center"/>
        <TextBox x:Name="emailTextBox" Grid.Row="3" Grid.Column="1" Width="200" Margin="0,0,0,10"/>

        <!-- Regisztrációs gomb -->
        <Button Grid.Row="4" Grid.Column="1" Content="Regisztráció" Width="100" Height="30" HorizontalAlignment="Left" Margin="0,10,0,0" Click="RegisterButton_Click"/>
    </Grid>
</Page>
